[package]
name = "rayon-hunt"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rayon = { path = "../../../vendor/rayon-patched", features = ["laplace"] }

[dev-dependencies]
laplace-probe-sdk = { path = "../../crates/laplace-probe-sdk", features = ["verification"] }
