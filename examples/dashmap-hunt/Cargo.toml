[package]
name = "dashmap-hunt"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
dashmap = { version = "6.1", features = ["laplace"] }

[dev-dependencies]
laplace-probe-sdk = { path = "../../crates/laplace-probe-sdk", features = ["verification"] }
