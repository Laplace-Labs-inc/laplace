[package]
name = "nonvendored-async-poc"
version = "0.1.0"
edition = "2021"
license.workspace = true
publish = false

[features]
fault-fixture = []

[dependencies]
laplace-sdk = { path = "../../crates/laplace-sdk" }
