[package]
name = "tokio-hunt"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tokio = { path = "../../vendor/tokio-patched/tokio", features = ["sync", "rt"] }

[dev-dependencies]
laplace-probe-sdk = { path = "../../crates/laplace-probe-sdk" }
