[package]
name = "deadpool-hunt"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]

[dev-dependencies]
deadpool = { version = "*", features = ["laplace", "managed"] }
laplace-probe-sdk = { path = "../../crates/laplace-probe-sdk" }
tokio = { workspace = true }
async-trait = "0.1"
